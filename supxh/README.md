# 肖恩AI 每日签到

自动完成「肖恩AI」（free.supxh.xin，免费大模型 API 站）的每日签到，领取 1000~3000 额度。

> 新用户注册立得 5000 点；每日签到再领 1000~3000 点。

## 文件

| 文件 | 说明 |
|---|---|
| `supxh.plugin` | **Loon 插件**（推荐）：设置里填账号密码即可，全自动 |
| `supxh.js` | 签到主脚本 |

## 使用方式

### 方式一：插件（推荐）⚡

在 Loon 中安装插件，然后在**插件设置**里填写账号与密码即可，脚本会自动登录获取 Cookie 再签到：

```
https://raw.githubusercontent.com/binasi/loon/main/supxh/supxh.plugin
```

| 参数 | 说明 |
|---|---|
| 账号 | 肖恩AI 账号或邮箱（**单账号**时填写） |
| 密码 | 肖恩AI 登录密码（**单账号**时填写） |
| 多账号(JSON) | **选填**；填写后优先，支持一次签到多个账号 |
| 调试模式 | 开启后日志记录请求状态与判定结果 |

### 多账号用法

在「多账号(JSON)」里填入账号列表，脚本会**逐个登录并签到**，最后汇总成一条通知推送。格式：

```json
[
  {"username": "账号1或邮箱1", "password": "密码1"},
  {"username": "账号2或邮箱2", "password": "密码2"}
]
```

> 注意：必须是严格的 JSON（英文双引号、逗号分隔）。填了多账号后，「账号/密码」两项会被忽略；想回到单账号，清空「多账号」即可。

### 方式二：自建配置（字符串 Cookie 或本地缓存）

不想用插件时，也可直接把完整 Cookie 作为字符串参数传入；参数留空则读取本地缓存（脚本登录成功后会自动写入）。

```ini
[Script]
cron "0 9 * * *" then script("https://raw.githubusercontent.com/binasi/loon/main/supxh/supxh.js") with tag="肖恩AI签到", timeout=60
```

## 维护记录

| 日期 | 变更 |
|---|---|
| 2026-09-20 | 新建，适配 free.supxh.xin 每日签到 |
| 2026-09-20 | 新增 Loon 插件（账号密码自动登录）；移除 Cookie 抓取脚本，目录统一到 `supxh/` |

## 已知限制

- **账号密码由 Loon 本地保管**：请勿分享含参数的插件配置截图。
- **签到额度当天有效**：今日签到额度当日未用部分次日清零；注册/邀请/兑换获得的固定额度长期保留。
- **一天一次**：当日已签到会返回「今天已经签到过了」并正常提示，不算失败。
- **登录态失效**：插件模式下 Cookie 过期会自动重新登录。
